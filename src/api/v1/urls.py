from django.conf.urls import include, url

from api.v1.views import *


urlpatterns = [
    url(r"^$",
        api_root,
        name="api-root"),

    # -------------------------------------------------------------------------
    # --- Authorization
    # -------------------------------------------------------------------------
    url(r"^auth/get-token/$",
        obtain_auth_token,
        name="obtain-auth-token"),

    # -------------------------------------------------------------------------
    # --- Version
    # -------------------------------------------------------------------------
    url(r"^app-version/$",
        app_version,
        name="app-version"),

    # -------------------------------------------------------------------------
    # --- Autocomplete
    # -------------------------------------------------------------------------
    url(r"^autocomplete/members/$",
        autocomplete_member_list,
        name="autocomplete-member-list"),

    # -------------------------------------------------------------------------
    # --- Accounts
    # -------------------------------------------------------------------------
    url(r"^accounts/", include("api.v1.api_accounts.urls")),

    # -------------------------------------------------------------------------
    # --- Blog
    # -------------------------------------------------------------------------
    url(r"^blog/", include("api.v1.api_blog.urls")),

    # -------------------------------------------------------------------------
    # --- Challenges
    # -------------------------------------------------------------------------
    url(r"^challenges/", include("api.v1.api_challenges.urls")),

    # -------------------------------------------------------------------------
    # --- Core
    # -------------------------------------------------------------------------
    url(r"^core/", include("api.v1.api_core.urls")),

    # -------------------------------------------------------------------------
    # --- Forum
    # -------------------------------------------------------------------------
    url(r"^foro/", include("api.v1.api_foro.urls")),

    # -------------------------------------------------------------------------
    # --- Home
    # -------------------------------------------------------------------------
    url(r"^home/", include("api.v1.api_home.urls")),

    # -------------------------------------------------------------------------
    # --- Invites
    # -------------------------------------------------------------------------
    url(r"^invites/", include("api.v1.api_invites.urls")),

    # -------------------------------------------------------------------------
    # --- Organizations
    # -------------------------------------------------------------------------
    url(r"^organizations/", include("api.v1.api_organizations.urls")),
]
